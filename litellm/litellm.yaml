# Static fallback config only. Runtime config is generated from Ollama /api/tags.
model_list: []

general_settings:
  master_key: ${LITELLM_MASTER_KEY}
  disable_spend_logs: true
  health_check_interval: 300

router_settings:
  routing_strategy: simple-shuffle
  num_retries: 2
  timeout: 180
